<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardCornerRadius="@dimen/cardCornerRadius"
    app:cardElevation="@dimen/cardElevation"
    android:layout_margin="@dimen/defaultLayoutMargin">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/ivItemProduct"
            android:layout_width="match_parent"
            android:layout_height="@dimen/heightImageProduct"
            android:src="@drawable/guantes"
            android:layout_margin="@dimen/defaultLayoutMargin"/>

        <TextView
            android:id="@+id/tvItemNameProduct"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Guantes Alpinestars GP"
            android:textStyle="bold"
            android:gravity="center_horizontal"
            android:textSize="@dimen/sizeNameProduct"
            android:textColor="@color/primaryTextColor"/>

        <TextView
            android:id="@+id/tvItemPriceProduct"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="$ 600.000"
            android:gravity="center_horizontal"
            android:textSize="@dimen/sizePriceProduct"
            android:textColor="@color/primaryTextColor"/>
    </LinearLayout>

</androidx.cardview.widget.CardView>